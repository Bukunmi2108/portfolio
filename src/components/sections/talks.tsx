import { Section } from "@/components/section";

export function Talks() {
  return (
    <Section id="talks" label="Talks & Press" title="Speaking">
      <p className="text-sm text-muted">
        Nothing here yet — this space is reserved for talks and press.
      </p>
    </Section>
  );
}
